meta {
  name: Get League by ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}/leagues/{{league_id}}
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("Get League by ID", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.id).to.equal(bru.getEnvVar("league_id"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
