meta {
  name: Get Scanner Jobs
  type: http
  seq: 2
}

get {
  url: {{base_url}}/scanner/jobs
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("Get Scanner Jobs", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.body).to.be.an("array");
  });
}
