meta {
  name: Get Filter by ID
  type: http
  seq: 3
}

get {
  url: {{base_url}}/filters/{{filter_id}}
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("Get Filter by ID", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.id).to.equal(bru.getEnvVar("filter_id"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
