meta {
  name: Health Check
  type: http
  seq: 2
}

get {
  url: {{base_url}}/../health
  auth: none
}

tests {
  test("Health Check", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.status).to.equal("ok");
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
