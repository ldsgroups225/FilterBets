meta {
  name: Get Fixture by ID
  type: http
  seq: 2
}

get {
  url: {{base_url}}/fixtures/{{fixture_id}}
  auth: bearer
}

auth:bearer {
  token: {{auth_token}}
}

tests {
  test("Get Fixture by ID", function() {
    expect(res.getStatus()).to.equal(200);
    expect(res.body.id).to.equal(bru.getEnvVar("fixture_id"));
  });
}

settings {
  encodeUrl: true
  timeout: 0
}
